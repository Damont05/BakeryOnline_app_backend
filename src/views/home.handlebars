<div class="container">

        <!--navBar-->
        <div class="nav-container">
            <nav class="navbar">
              <div class="navbar-logo">
                <!--abajo el logo-->
              </div>
              <a href="/chat">Chat</a>
              <h1 class="getCart" id="getCart">🧺<span id="cantCart" class="cant-cart"></span></h1>
            </nav>
          </div>
      
          <!--banner-->
          <div class="banner">
            <div class="banner-container">
                
              <h1>Entreamasados</h1>
              <p>Panaderia online, del horno a tu puerta</p>
            </div>
          </div>
        <!--content-->
        <div id="content" class="content-style">
            {{#each products}}
                  <div class="card">
                      <img src="{{this.thumbnail}}">
                      <h3 class="name-product">{{this.title}}</h3>
                      <h6 class="weight">{{this.description}}</h6>
                      <hr>
                      <div class="price">${{this.price}}.0</div>
                      <div class="agregar">
                          <button>Agregar</button>
                      </div>
                  </div>
            {{/each}}
        </div>
        <div class="paginate">
            <a>{{page}} de {{totalPages}}</a>
            {{#if hasPrevPage}}
              <a href="/?pag={{prevPage}}">⬅️</a>
            {{/if}}
            {{#if hasNextPage}}
              <a href="/?pag={{nextPage}}">➡️</a>
            {{/if}}
        </div>

        <!--cart-container-canasta-->
        <div id="cart-container" class="cart-container"></div>
        <!--cart-container-pay-->
        <div id="container-pay" class="cart-container"></div>

    </div>

    <div id="sweetAlert"></div>

 <script type="module" src="js/home.js"></script> 
